import { PreviewStaticPipe } from './preview-static.pipe';

describe('PreviewStaticPipe', () => {
  it('create an instance', () => {
    const pipe = new PreviewStaticPipe();
    expect(pipe).toBeTruthy();
  });
});
