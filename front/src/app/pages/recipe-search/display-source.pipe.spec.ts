import { DisplaySourcePipe } from './display-source.pipe';

describe('DisplaySourcePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplaySourcePipe();
    expect(pipe).toBeTruthy();
  });
});
