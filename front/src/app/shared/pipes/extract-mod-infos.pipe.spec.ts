import {ExtractModInfosPipe} from './extract-mod-infos.pipe';

describe('ExtractModInfosPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractModInfosPipe();
    expect(pipe).toBeTruthy();
  });
});
